# 校园自行车智能检测系统：实时优化与边缘部署路线图

> 说明：该文档用于把“目标检测 + 追踪 + 统计 + 边缘部署”的任务拆成可执行方案，便于组会汇报、比赛答辩和论文撰写。

## 1. 项目目标（统一口径）

一句话：

**基于 YOLO 检测与多目标追踪，实现校园场景下自行车的实时识别、去重计数、流量统计与停放密度分析，并支持边缘端部署。**

核心能力：

- 检测：定位每一帧中的自行车框。
- 追踪：为每辆自行车分配稳定 ID，尽量减少 ID Switch 与跟踪丢失。
- 统计：输出当前数量、峰值数量、进出流量、区域密度。
- 展示：视频画框叠加 + 终端/日志实时指标 + 离线统计报告。

## 2. 实时优化清单（优先级从高到低）

## 2.1 模型轻量化优先

- 模型优先使用 `yolov8n`（不要盲目切到 m/l/x）。
- 输入尺度从 `640` 降到 `320/416/448`，在精度可接受的前提下优先速度。

建议做一次三档对比：

- 档位 A：`imgsz=320`
- 档位 B：`imgsz=416`
- 档位 C：`imgsz=640`

记录每档的 `FPS / mAP / IDF1 / MOTA`，用于汇报展示“速度-精度折中曲线”。

## 2.2 关闭高开销功能

实时演示阶段建议关闭：

- 密集检测（dense detector）
- 多尺度推理
- 视频写盘保存
- 复杂轨迹可视化（仅保留框 + ID + 必要统计）

## 2.3 推理循环去阻塞

- `cv2.waitKey(1)`（保持最小等待）
- 不做任何主动 `sleep`
- 摄像头读帧、推理、绘制分离，避免单环节阻塞

## 2.4 建议监控指标

至少实时打印：

- 当前 FPS（滑动平均）
- GPU 占用/显存
- 当前在场数
- 累计进出计数
- 跟踪质量（ID switch 次数）

## 3. 边缘部署路线（可直接写“方法章节”）

## 3.1 轻量化训练

- 基础模型：YOLOv8n
- 研究策略：
  - 知识蒸馏（teacher: v8s/v8m, student: v8n）
  - 结构化剪枝
  - 量化（INT8，建议 PTQ 起步，QAT 进阶）

产出：不同压缩率下的精度与速度对比表。

## 3.2 模型格式转换

- `.pt -> ONNX`
- ONNX ->
  - TensorRT（Jetson/NVIDIA）
  - RKNN（RK3588）
  - NCNN/TFLite（ARM 通用）

## 3.3 平台侧推理引擎

- Jetson Nano/Xavier/Orin：TensorRT
- Raspberry Pi 4/5：NCNN 或 TFLite
- RK3588：RKNN Toolkit

## 3.4 设备独立运行

- 启动即加载模型
- 直连 USB/CSI 摄像头
- 本地显示与日志落盘
- 可选 MQTT/HTTP 上传统计数据

## 4. 学期节奏建议（16 周）

- 第 1-3 周：跑通检测/追踪与可视化，建立基线指标。
- 第 4-7 周：蒸馏+剪枝+量化实验，形成轻量化对比。
- 第 8-12 周：ONNX 与端侧引擎部署，完成设备实测。
- 第 13-16 周：竞赛材料、论文报告、答辩 DEMO 与视频。

## 5. 验收指标模板（建议直接贴到报告）

- 实时性：
  - PC（RTX 5070Ti）：`>= 80 FPS`（目标 100+）
  - 边缘端（按设备分层设定，如 15/25/30 FPS）
- 精度：`mAP50` 与 `mAP50-95`
- 跟踪：`IDF1 / MOTA / HOTA / ID Switch`
- 业务：
  - 数量统计误差（与人工标注对比）
  - 流量统计误差（进/出方向）
  - 停放密度热区稳定性

## 6. 最小可用配置（实时演示推荐）

- 模型：`yolov8n`
- 输入：`imgsz=416`（若卡顿降到 320）
- 追踪：BoT-SORT（默认参数起步，再按遮挡场景调参）
- 功能：只保留框、ID、数量与流量；关闭视频保存和高级可视化
- 目标：稳定低延迟 + 连续长时运行（例如 2 小时不崩溃）

## 7. 可直接用于组会的一句话总结

**先用 YOLOv8n + BoT-SORT 在 GPU 上把实时性跑稳，再通过蒸馏/剪枝/INT8 与 TensorRT/RKNN 完成边缘部署，最后以速度-精度-功耗三维指标完成竞赛与论文闭环。**
